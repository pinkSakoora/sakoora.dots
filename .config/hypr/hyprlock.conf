source = ~/.cache/wal/colors-hyprland.conf

###################
### INPUT FIELD ###
###################

input-field {
    monitor =
    size = 300,60
    outline_thickness = 0
    dots_center = false
    inner_color = rgba(0,0,0,0)
    check_color = $foreground
    fail_color = $background
    fail_text = 
    font_family = FontAwesome
    font_color = $background
    swap_font_color = true
    fade_on_empty = false
    rounding = 10
    placeholder_text = 
    position = 563, -495
    halign = left
    valign = top
    zindex = 4
}

background {
monitor =
path = ~/.cache/hyprlock-cache/beforelock.png
blur_passes = 2
}

animations {
enabled = true

bezier = easeOutQuint,0.23,1,0.32,1.0
bezier = almostLinear,0.5,0.5,0.75,1.0
bezier = stopGo,0.5,0,1.0,0.5

animation = inputFieldDots, 1, 10.21, easeOutQuint
animation = fadeIn, 1, 6.21, almostLinear
animation = fadeOut, 1, 7.21, stopGo
animation = inputFieldColors, 1, 5, easeOutQuint
}

# Left top panel

image {
monitor =
path = ~/.cache/hyprlock-cache/lefttoppanel.png
size = 480
rounding = 50
border_size = 0
position = 460, -200
halign = left
valign = top
shadow_passes = 2
}

# Left bottom panel

image {
monitor =
path = ~/.cache/hyprlock-cache/leftbottompanel.png
size = 160
rounding = 50
border_size = 0
position = 460, -720
halign = left
valign = top
shadow_passes = 2
}

# Right panel

image {
monitor =
path = ~/.cache/hyprlock-cache/finalpanel.png
size = 480
rounding = 50
border_size = 0
position = 980, -120
halign = left
valign = top
shadow_passes = 2
}

# Top "field"

shape {
monitor =
size = 370, 60
color = $color2
rounding = 50
position = 511, -415
halign = left
valign = top
zindex = 1
}

# User symbol holder

shape {
monitor =
size = 50, 50
color = rgba(255,255,255, 0.3)
rounding = 50
position = 516, -420
halign = left
valign = top
zindex = 2
}

# Bottom "field"

shape {
monitor =
size = 370, 60
color = $color2
rounding = 50
position = 511, -495
halign = left
valign = top
zindex = 1
}

# Lock symbol holder

shape {
monitor =
size = 50, 50
color = rgba(255,255,255, 0.3)
rounding = 50
position = 516, -500
halign = left
valign = top
zindex = 2
}

# Brightness symbol holder

shape {
monitor =
size = 100, 100
color = $color2
rounding = -1
position = 1320, -820
halign = left
valign = top
zindex = 2
onclick = "/home/$USER/.config/scripts/brightnessbutton"
}

# User symbol

label {
monitor =
text = <span>  </span>
color = $background
font_size = 16
font_family = FontAwesome
position = 519, -433
halign = left
valign = top
zindex = 4
}

# Brightness symbol

label {
monitor =
text = <span>  </span>
color = $foreground
font_size = 36
font_family = FontAwesome
position = 1319, -845
halign = left
valign = top
zindex = 4
}

# Lock symbol

label {
monitor =
text = <span>  </span>
color = $background
font_size = 16
font_family = FontAwesome
position = 519, -512
halign = left
valign = top
zindex = 4
}

# Username

label {
monitor =
text = <span font_weight="bold">$USER</span>
color = $background
font_size = 18
font_family = Josefin Sans
position = 580, -435
halign = left
valign = top
zindex = 4
}

# Quote

label {
monitor =
text = cmd[update:60000] ~/.config/scripts/hyprlock-run/quote
text_align = left
color = $foreground
font_size = 12
font_family = Noto Sans
position = 510, -575
halign = left
valign = top
zindex = 1
}

# Host name

label {
monitor =
text = cmd[update:60000] cat /proc/sys/kernel/hostname
text_align = left
color = $foreground
font_size = 16
font_family = Josefin Sans
position = 512, -245
halign = left
valign = top
zindex = 1
}

# Login text

label {
monitor =
text = <span font_weight="light"> Log in </span>
text_align = left
color = $foreground
font_size = 36
font_family = Josefin Sans
position = 495, -355
halign = left
valign = top
zindex = 1
}

# Uptime text

label {
monitor =
text =  UPTIME
color = $foreground
font_size = 20
font_family = Josefin Sans
position = 510, -735
halign = left
valign = top
zindex = 1
}

# Actual uptime

label {
monitor =
text = cmd[update:60000] uptime -p | sed 's/up//' | sed 's/ hours/H/' | sed 's/, //' | sed 's/ minutes/M/' | sed 's/ hour/H/' | sed 's/ minute/M/'
text_align = right
color = $color2
font_size = 20
font_family = Fira Mono
position = -1160, -732
halign = right
valign = top
zindex = 1
}

# Current time

label {
monitor =
text = <span>$TIME</span>
text_align = left
color = $foreground
font_size = 84
font_family = Josefin Sans
position = 505, -768
halign = left
valign = top
zindex = 1
}

# Day

label {
monitor =
text = cmd[update:8640000] date +%a | sed 's/[a-z]/\U&/g'
color = $foreground
font_size = 48
font_family = Josefin Sans
position = 185, -280
halign = center
valign = top
zindex = 4
}

# Date

label {
monitor =
text = cmd[update:8640000] date +"%d %B, %Y"
color = $foreground
font_size = 16
font_family = Josefin Sans
position = 185, -340
halign = center
valign = top
zindex = 4
}

# Network indicator

label {
monitor =
text = cmd[update:30000] .config/scripts/hyprlock-run/networkcheck
color = $foreground
font_size = 16
font_family = FontAwesome
position = 1070, -780
halign = left
valign = top
zindex = 4
}

# Battery indicator

label {
monitor =
text = cmd[update:30000] .config/scripts/hyprlock-run/batterycheck
color = $foreground
font_size = 16
font_family = FontAwesome
position = 1160, -780
halign = left
valign = top
zindex = 4
}

# Grace indicator

label {
monitor =
text = cmd[update:60000] .config/scripts/hyprlock-run/grace
color = $foreground
font_size = 24
font_family = FontAwesome
position = 839, -780
halign = left
valign = top
zindex = 4
}
